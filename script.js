document.addEventListener("DOMContentLoaded", () => {
  const matchesContainer = document.getElementById("matches");
  matchesContainer.innerHTML = "<p>Fetching live matches...</p>";
  // API integration will go here
});
